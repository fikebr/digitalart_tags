# this is a module that contains tools that I use to manage my fooocus workflow

def get_metadata_for_folder(filename):


    pass

get get_metadata_for_file(filename):
    # test file exists
    # test if this file is a child of an original
    # does the *.json already exist
    # if yes then does the fooocus metadata already exist
    # get folder for file
    # get log.html
    # get div id
    # use bsoup to get metadata
    # save the metadata to a json file

    if os.path.exists(filename):